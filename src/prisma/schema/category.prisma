model Category {
  id        String    @id @default(uuid()) @db.Uuid
  name      String    @unique
  products  Product[]
  description String?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt

  @@map("categories")
}